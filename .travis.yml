language: js

script:
  - echo "no build required"

deploy:
  - provider: lambda
    function_name: "lambda-travis-js"
    runtime: "nodejs8.10"

    # AWS Region (Ireland)
    region: "eu-west-1"
    # AWS IAM
    role: "arn:aws:iam::854014537498:role/lambda_basic_execution"

    # Look out: filename ist statically set to index and will be prepended!
    handler_name: "call"
    # deploy on branch js
    on: js
